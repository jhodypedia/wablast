

<h3 class="mb-3"><i class="fa fa-gear me-1"></i> Pengaturan</h3>

<div class="card reveal">
  <div class="card-body">
    <form method="post">
      <div class="mb-3">
        <label>Nama Website</label>
        <input type="text" name="site_name" class="form-control" value="<%= siteName %>">
      </div>
      <div class="form-check form-switch mb-3">
        <input class="form-check-input" type="checkbox" role="switch" id="regSwitch" name="register_enabled" value="true" <%= registerEnabled==='true' ? 'checked' : '' %>>
        <label class="form-check-label" for="regSwitch">Aktifkan Registrasi</label>
      </div>
      <button class="btn btn-success">Simpan</button>
    </form>
  </div>
</div>
